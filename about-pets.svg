import Link from "next/link";

export default function Hero() {
  return (
    <section className="hero container">
      <div className="hero-copy">
        <span className="eyebrow">Dogs + cats • subscription + one-time</span>
        <h1>Pet boxes built around joy, play, and the kind of treats they actually love.</h1>
        <p>
          PetBox is a pet-first storefront for monthly and quarterly boxes,
          one-time add-ons, and a guided configurator that lets customers build the right box for their pet.
        </p>
        <div className="hero-actions">
          <Link href="/configure" className="btn">Build a Box</Link>
          <Link href="/shop" className="btn btn-secondary">Shop Add-ons</Link>
        </div>
      </div>
      <div className="hero-card">
        <img src="/images/hero-pets.svg" alt="Pets with boxes" />
        <div className="hero-stats">
          <div><strong>2 box plans</strong><span>Monthly + quarterly</span></div>
          <div><strong>Dogs + cats</strong><span>Species-aware products</span></div>
          <div><strong>Built for mobile</strong><span>Fast and phone friendly</span></div>
        </div>
      </div>
    </section>
  );
}
